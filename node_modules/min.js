exports.min = (arr) => {

    const minValue = Math.min(...arr);

    if (!minValue) {
        console.log('Ви вели не коректні дані');
    } else {
        console.log(`Мінімальне значення: ${minValue} з ${arr}`);
    }
}